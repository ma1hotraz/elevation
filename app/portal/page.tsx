import { PortalApp } from "./PortalApp";

export const metadata = {
  title: "Elevation Portal",
  description: "Admin and student portal for Elevation test links and materials.",
};

export default function PortalPage() {
  return <PortalApp />;
}
